using UnityEngine;
using System.Collections;

public class PNJ : parametre {
	protected string nom;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		if (Input.GetKey (discussion)) {
			// demmarage du dialogue
		} 
	}
}
